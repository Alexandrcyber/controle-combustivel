# Exemplo de configuração de variáveis de ambiente para o frontend

# URL base da API (para produção no Netlify)
API_BASE_URL=https://controle-combustivel.onrender.com/api

# URL base do backend (para produção no Netlify)
BACKEND_URL=https://controle-combustivel.onrender.com

# Ambiente de execução
NODE_ENV=production

# NOTA: A variável NETLIFY é reservada pelo sistema e é detectada automaticamente
# Não adicione NETLIFY=1 nas variáveis de ambiente do painel do Netlify
